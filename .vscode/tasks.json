{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "docker-build",
			"label": "docker-build",
			"platform": "node",
			"dockerBuild": {
				"dockerfile": "${workspaceFolder}/Cadastro_Curriculos/Dockerfile",
				"context": "${workspaceFolder}/Cadastro_Curriculos",
				"pull": true
			},
			"node": {
				"package": "${workspaceFolder}/Cadastro_Curriculos/package.json"
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run: release",
			"dependsOn": [
				"docker-build"
			],
			"platform": "node",
			"node": {
				"package": "${workspaceFolder}/Cadastro_Curriculos/package.json"
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run: debug",
			"dependsOn": [
				"docker-build"
			],
			"dockerRun": {
				"env": {
					"DEBUG": "*",
					"NODE_ENV": "development"
				}
			},
			"node": {
				"package": "${workspaceFolder}/Cadastro_Curriculos/package.json",
				"enableDebugging": true
			}
		}
	]
}